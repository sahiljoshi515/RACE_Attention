# Inference-only deps for the RACE-nanochat chat launcher.
# Install torch from the CUDA 12.8 index:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu128
torch==2.9.1
tiktoken
filelock
huggingface_hub>=0.24
ninja
setuptools
wheel
